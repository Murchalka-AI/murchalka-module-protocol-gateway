# Security

Report vulnerabilities privately. Do not include credentials, external payloads, signing keys, or audit data in reports, logs, fixtures, or bundles. The gateway is default-deny, strips non-allowlisted headers, bounds bodies and concurrency, and never performs outbound network requests.
